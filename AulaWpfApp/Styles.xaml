<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Bảng màu chủ đạo: Dark Gothic / Ancient Ruins -->
    <SolidColorBrush x:Key="BgDarkest" Color="#0B0F0D"/>
    <SolidColorBrush x:Key="BgPanel" Color="#151A17"/>
    <SolidColorBrush x:Key="BgPanelLight" Color="#1E251F"/>
    <SolidColorBrush x:Key="AccentGold" Color="#C9A24B"/>
    <SolidColorBrush x:Key="AccentGoldDim" Color="#8A6D3B"/>
    <SolidColorBrush x:Key="TextPrimary" Color="#E8E4D8"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#A8A296"/>
    <SolidColorBrush x:Key="BorderMist" Color="#3A3F38"/>
    <SolidColorBrush x:Key="StatusOk" Color="#8FBF6E"/>
    <SolidColorBrush x:Key="StatusBad" Color="#C0604A"/>

    <!-- Font tiêu đề kiểu cổ điển -->
    <FontFamily x:Key="TitleFont">Cambria</FontFamily>

    <!-- Style nút chung: nền tối, chữ vàng đồng, viền mờ, phát sáng khi hover -->
    <Style x:Key="GothicButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BgPanel}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderMist}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,6"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentGold}"/>
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BgPanelLight}"/>
                            <Setter TargetName="border" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#C9A24B" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource AccentGoldDim}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Style nút sidebar (dạng menu dọc bên trái) -->
    <Style x:Key="SidebarButton" TargetType="Button" BasedOn="{StaticResource GothicButton}">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Margin" Value="15,4"/>
        <Setter Property="Height" Value="42"/>
    </Style>

</ResourceDictionary>